<aura:component controller = "resendOTPController" implements="force:appHostable,flexipage:availableForAllPageTypes,flexipage:availableForRecordHome,force:hasRecordId,forceCommunity:availableForAllPageTypes,force:lightningQuickAction" access="global" >
    <aura:attribute name="name" type="string"/>
    <aura:attribute name="project" type="string"/>
    <aura:attribute name="otp" type="string"/>
    <aura:attribute name="simpleRecord" type="Object"/>
    <aura:attribute name="recordError" type="String"/>
    <aura:attribute name="isLoaded" type="Boolean"/>
    <force:recordData aura:id="recordLoader"
                      fields="Lead_Name__c,Source_OTP__c,Phone__c,Allocated_Project__c"
                      recordId="{!v.recordId}"
                      targetFields="{!v.simpleRecord}"
                      targetError="{!v.recordError}"
                      recordUpdated="{!c.doInit}"
                      />
    <div>
        <lightning:recordEditForm recordId="{!v.recordId}" objectApiName="Related_Source__c">
            <div class="slds-grid">
                <div class="slds-col slds-size_1-of-2">
                    <lightning:inputField fieldName="Lead_Name__c" disabled="true" />
                </div>
                &nbsp;
                <div class="slds-col slds-size_2-of-2">
                    <lightning:inputField fieldName="Allocated_Project__c" disabled="true"/>
                </div>
            </div>
            <div class="slds-grid">
                <div class="slds-col slds-size_1-of-2">
                    <lightning:inputField fieldName="Source__c" disabled="true" />
                </div>
                &nbsp;
                <div class="slds-col slds-size_2-of-2">
                    <lightning:inputField fieldName="CP_Id__c" disabled="true"/>
                </div>
            </div>
        </lightning:recordEditForm>
    </div>
    <br/>
    <br/>
    <br/>
    <div>
         <button class="slds-button slds-button_brand" onclick="{!c.sendOTP}">Send OTP</button>
    </div>
</aura:component>